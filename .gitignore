# ── Données du datalake & sorties générées ──────────────────
#    (bronze/silver/gold, _logs, _reference/airports.dat téléchargé…)
datalake/
.datalake/
data/

# ── Secrets / surcharges locales  (garder .env.example) ─────
.env
.env.local
.env.*.local

# ── Python ──────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg

# ── Environnements virtuels ─────────────────────────────────
.venv/
venv/
env/
ENV/

# ── Spark (artefacts locaux) ────────────────────────────────
.spark/
spark-warehouse/
metastore_db/
derby.log

# ── Airflow (si lancé en local hors conteneur ; les DAGs restent suivis) ──
airflow/logs/
airflow.cfg
airflow.db
webserver_config.py
standalone_admin_password.txt
simple_auth_manager_passwords.json.generated

# ── Tests & qualité de code ─────────────────────────────────
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# ── Jupyter ─────────────────────────────────────────────────
.ipynb_checkpoints/

# ── IDE / OS ────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Logs & temporaires ──────────────────────────────────────
*.log
*.tmp
*.bak
*.pid
.tmp/
