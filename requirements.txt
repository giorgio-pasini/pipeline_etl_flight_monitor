# Dépendances runtime du pipeline ETL trafic aérien

# Spark + lecture/écriture Parquet
pyspark==3.5.0
pandas==2.1.0
pyarrow>=12.0          # lecture Parquet pandas (dashboard KPIs Gold, sans Spark)

# API FlightRadar24
FlightRadarAPI==1.5.1
beautifulsoup4==4.14.3
curl-cffi==0.15.0

# Dashboard
streamlit==1.30.0

# Tests :    pip install -r requirements-dev.txt
